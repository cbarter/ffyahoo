import {useState} from "react";
import useApiContext from "../hooks/useApiContext";

function TeamShow({team}) {

    //TODO: implement roster editing with use of context exposed callbacks

    return (
        <div>
            //TODO: pass players within team to player component
        </div>
    )
}

export default TeamShow;
